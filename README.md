# ReactCinema

1. npm install babel webpack -g
2. npm install
3. ./src/utils/KEYS.js
```
let KEYS = {
  API_KEY: "YOUR KEY"
}

export default KEYS;
}
```

4. npm start
Open your browser and go to http://localhost:8888
